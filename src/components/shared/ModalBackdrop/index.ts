import {
  IModalBackdropProps as _IModalBackdropProps,
  ModalBackdrop as _ModalBackdrop
} from './ModalBackdrop';

export type IModalBackdropProps = _IModalBackdropProps;
export const ModalBackdrop = _ModalBackdrop;
